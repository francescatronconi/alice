import { ChallengeViewDirective } from './challenge-view.directive';

describe('ChallengeViewDirective', () => {
  it('should create an instance', () => {
    const directive = new ChallengeViewDirective();
    expect(directive).toBeTruthy();
  });
});
